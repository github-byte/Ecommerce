import React, { useState } from 'react';

const MainContext = (props) => {

    const MainContextLayer = React.createContext<null>(null);
    return (
        <div>
            
        </div>
    );
}

export default MainContext;